---
const { current = 0, total = 0 } = Astro.props;
---

<div class="counter-container fixed top-6 right-6 z-50">
  <div class="flex items-center gap-3">
    <span
      id="current-count"
      class="text-5xl md:text-8xl font-bold text-amber-400 font-mouse text-shadow"
      >{current}</span
    >
    <span class="text-white/40 text-3xl md:text-6xl">/</span>
    <span
      id="total-count"
      class="text-3xl md:text-6xl font-mouse font-semibold text-fuchsia-400/80"
      >{total}</span
    >
  </div>
</div>

<style>
  .counter-container {
    animation: fadeInDown 0.8s ease-out 0.2s both;
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
